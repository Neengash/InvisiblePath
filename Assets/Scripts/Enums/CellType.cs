public enum CellType
{
    EMPTY,
    ALWAYS,
    MIRROR,
    HOLE,
    ROTATION,
}

public enum CellTypeDetailed
{
    EMPTY,
    ALWAYS_TOP,
    ALWAYS_RIGHT,
    ALWAYS_BOT,
    ALWAYS_LEFT,
    MIRROR_UP,
    MIRROR_DOWN,
    HOLE,
    ROTATION_CLOCK,
    ROTATION_COUTNERCLOCK,
}

